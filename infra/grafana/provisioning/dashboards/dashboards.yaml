apiVersion: 1
providers:
  - name: OTT Feed
    orgId: 1
    folder: OTT Feed
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options: {path: /etc/grafana/provisioning/dashboards}
